# Rust
target/
*.pdb

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
.venv/
env/

# IDE
.vscode/
.idea/
.claude/settings.local.json
.claude/scheduled_tasks.lock
*.swp
*.swo
*~
*.code-workspace

# Local-only Claude / dev notes (machine-specific paths, NAS workflows, etc.)
CLAUDE.local.md

# OS
.DS_Store
Thumbs.db
nul

# Runtime data
data/

# Build artifacts
*.o
*.a
*.exe
*.dll

# Logs
*.log

# Test databases (RocksDB artifacts)
*.db/

# Environment / secrets
.env*

# Bridge (Python venv + deps)
jiminy-bridge/

# Model files
models/
*.gguf
jiminy-bridge/.venv/

# Frontend (SvelteKit / Tauri)
frontend/node_modules/
frontend/build/
frontend/.svelte-kit/

# RAG chatbot index (deployed to HF Space, not tracked in git)
sovereign-chatbot/chunks.json
sovereign-chatbot/embeddings.npy

# Internal docs — must NOT reach the public github mirror
website/marketing-plan.md

# Local-only security tooling — red-team workflow + its reports (never publish)
.claude/workflows/
.claude/security-audits/
# Safety net: red-team scratch artifacts must never be committed
**/zz_scratch_*.rs
**/zzz_scratch_*.rs
*_probe_out.txt
